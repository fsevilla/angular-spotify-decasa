import { ProgressStatusDirective } from './progress-status.directive';

describe('ProgressStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressStatusDirective();
    expect(directive).toBeTruthy();
  });
});
