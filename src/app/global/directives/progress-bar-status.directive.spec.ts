import { ProgressBarStatusDirective } from './progress-bar-status.directive';

describe('ProgressBarStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressBarStatusDirective();
    expect(directive).toBeTruthy();
  });
});
